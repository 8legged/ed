<% provide(:title, 'Lessons') %>
<h4>Lessons</h4>

<p>
  Show available categories<br />
  Need sorting by views, e.t.c., as well as filtering by duration, e.t.c.
</p>
 <table class="table">
  <thead>
    <tr>
      <th>action</th>
      <th>Title</th>
      <th>Description</th>
      <th>Category</th>
    </tr>
  </thead>
  <tbody>
   <% @lesson.each do |lesson| %>
   <tr>
    <td><%= link_to 'view', lesson , class: "btn btn-default" %></td>
    <td><%= lesson.title %></td>
    <td><%= truncate(lesson.description, length: 100) { link_to "(read more)", lesson } %></td>
    <td><%= lesson.category %></td>
    </td>
  </tr>
  <% end %>
</tbody>
</table>
